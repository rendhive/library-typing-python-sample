from typing import Dict

def get_user_info() -> Dict[str, str]:
    return {"name": "Alice", "age": "30"}

user_info = get_user_info()
print(user_info)
# Fungsi: Mengembalikan informasi pengguna sebagai dictionary.
# Kondisi: Ketika Anda perlu menjelaskan struktur data yang kompleks.
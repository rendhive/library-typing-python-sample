from typing import List

def print_numbers(numbers: List[int]) -> None:
    for number in numbers:
        print(number)

print_numbers([1, 2, 3, 4, 5])
# Fungsi: Menerima list angka dan mencetak setiap angka.
# Kondisi: Ketika Anda ingin mendefinisikan tipe list dari elemen yang bisa diterima.